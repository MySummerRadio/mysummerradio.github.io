media:
  input: public/
  output: /
content:
  - name: posts
    label: Blog
    type: collection
    path: src/content/blog
    fields:
      - name: title
        type: string
      - name: pubDate
        type: date
        options:
          format: "yyyy-MM-dd"
      - name: published
        type: boolean
        default: true
      - name: description
        type: string
      - name: tags
        type: object
        list: true
        fields:
          - name: tag
            type: string
      - name: body
        type: rich-text
    view:
      fields: [title, pubDate, published]
      sort: [pubDate, title]
      default:
        sort: pubDate
        order: desc
  - name: sidebars
    label: Sidebars
    type: group
    items:
      - name: left
        label: Left
        type: file
        path: src/content/sidebar-left.yaml
        fields:
          - name: title
            type: string
          - name: sections
            label: Sections
            type: object
            list:
              collapsible:
                collapsed: true
                summary: "{heading} ({index})"
            fields:
              - name: heading
                type: string
              - name: text
                type: rich-text
      - name: right
        label: Right
        type: file
        path: src/content/sidebar-right.yaml
        fields:
          - name: title
            type: string
          - name: sections
            label: Sections
            type: object
            list:
              collapsible:
                collapsed: true
                summary: "{heading} ({index})"
            fields:
              - name: heading
                type: string
              - name: text
                type: rich-text
  - name: about
    label: À propos
    type: file
    path: src/pages/about.md
    fields:
      - name: title
        type: string
      - name: body
        type: rich-text
settings:
  content:
    merge: true
  commit:
    identity: app
    templates:
      create: "chore(create): {path}"
      update: "chore(update): {path}"
      delete: "chore(delete): {path}"
      rename: "chore(rename): {oldPath} -> {newPath}"
